# Images

Drop your profile photo here as `avatar.png` (a square image works best —
roughly 400×400 px). It's referenced from `_config.yml` and shows in the
sidebar. Put your CV at `../cv.pdf` (i.e. `assets/cv.pdf`).
